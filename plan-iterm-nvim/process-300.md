<!-- AUTO-GENERATED by /make-plan. Execution core. -->
# Process 300: 実行後の知見永続化

## Implementation Brief（コピペ用）

- **背景**: iTerm2 AppleScript と tmux client の対応は環境依存で、実機検証の発見を次回へ残す必要がある。
- **目的**: 実装・実機確認で確定した前提、失敗例、再現手順を持続的な知見として記録する。
- **変更範囲**: `.serena` memory、`stigmergy`、必要時 `MEMORY.md`。プロダクトコードは変更しない。
- **禁止事項**: 実機未確認の仮説を事実として保存しない。機密の Ex コマンド本文、TTY、作業ディレクトリを保存しない。

## Symbol Targets

- doc-only。`patch_only: true`。`disjoint_guarantee: n/a`。事前確認: `git_clean`。

## Behavior Specification

対象外: 実装後の知識記録のみ。

## Verification Gates

| gate_id | phase | type | executor | required | command | input_scope | pass_criteria | evidence_spec | failure_policy | criterion_refs | policy_refs |
|---|---|---|---|---|---|---|---|---|---|---|---|
| P300-VG-knowledge | refactor | review | agent | false | 知見記録の確認 | P01–P200 evidence | 事実と仮説が分離される | 記録パス | retry_budget_source=task_retry_budget; await_input | SC-04 | maintainability |

## Knowledge Phase

- [ ] `mcp__serena__write_memory` で再利用可能な知見を保存する。
- [x] `stigmergy/` にパターンまたは失敗モードを記録する。
- [x] 必要性が高い時だけ `MEMORY.md` を更新する。
- [ ] `brv` が利用可能なら、機密情報を含まない要約だけを保存する。

## Manual Verification

対象外: 実行結果の証跡を再利用する作業であり、別の GUI 検証は不要。

## Dependencies

- Requires: 10, 200
- Blocks: -
